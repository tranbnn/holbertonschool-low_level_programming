#include "main.h"
/**
 * Description: _islower - checks for lowercase characters
 * Return: 1 if lowercase or 0 if otherwise
 */

int _islower(int c)
{
	if (_islower(c))
	{
		_putchar(c);
		return (1);
	}
	else
	{
		_putchar(c);
		return (0);
	}
}
