#include "main.h"
/**
 * Description: _abs - computes absolute value of an integer
 * @n: the integer to check
 * Return: The absolute value of the integer
 */
int _abs(int n)
{
        if (n >= 0)
        {
                return (n);
        }
        return (-n);
}
